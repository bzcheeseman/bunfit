#ifndef GET_DATA_HPP
#define GET_DATA_HPP

#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <map>

#include "math.h"

#include "main.hpp"

// This assumes that you're operating in the directory with the dataset inside it
//OR give the full path to the function

// NEED TO FIX TO WORK WITH NEW PROGRAM

bool get_data(dataSet<double> *);

#endif // GET_DATA_HPP
